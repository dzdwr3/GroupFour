package cn.group4.tests;

import org.openqa.selenium.WebDriver;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

import cn.group4.action.CheckHome;
import cn.group4.utils.DriverFactory;
import cn.group4.utils.UITest;

public class ZentaoTest extends UITest {
	WebDriver driver = DriverFactory.getChromeDriver();

	@BeforeMethod(alwaysRun = true)
	public void init() {
   
		super.init(driver);
		CheckHome.setDriver(driver);
	}

	@Test
	public void loginByUerName() throws Exception {
		CheckHome.check();
	}

//	@AfterMethod(alwaysRun = true)
//	public void stop() {
//		super.stop();
//	}
}
